### Prompt 03: Seguir tasks.md em ordem (a ia vai ler proposal.md (design.md...).. atualizar sattus.

```bash
/openspec:apply
```

ou

```bash
Implemente a mudança [change-id]"
```

Ex.:

```bash
Implemente a mudança [add-user-registration]"
```

### O que eu fiz?

Apliquei o comando para aprovar implementação.
o aegnte:
Eu:
Ler proposal.md para entender o objetivo
Ler tasks.md para ver a checklist
Implementa cada tarefa sequencialmente
Executa testes para validar
Atualiza tasks.md marcando as tarefas como [x]

**Seu papel**: Revisar commits, testar, aprovar

Neste ponto, a alteração foi implementada, mas ainda não é considerada uma "especificação" no painel de controle.

**Dica:**Consulte o painel do OpenSpec: openspec view

### Git

```bash
git add .
git commit -m"feat: openspec:apply add-user-registration"
```
