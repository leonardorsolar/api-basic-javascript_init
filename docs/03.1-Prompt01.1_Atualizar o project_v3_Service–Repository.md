### Prompt 02: Crie a sua primeira proposta(proposal) de alteração e validá-la

```bash
Atualize o arquivo openspec/project.md com as seguintes alterações:

1. Adicione a tecnologia [TECNOLOGIA] na stack
2. Inclua a convenção de [CONVENÇÃO ESPECÍFICA]
3. Atualize a estrutura de pastas com [NOVA ESTRUTURA]

Mantenha as informações existentes e adicione/modifique apenas o necessário.
```

Ex.:

````bash
Atualize o arquivo `openspec/project.md` com as seguintes alterações:

1. Adicione a tecnologia **[TECNOLOGIA]** na stack.
2. Inclua a convenção **[CONVENÇÃO ESPECÍFICA]**.
3. Atualize a estrutura de pastas para o padrão **Feature-First (Vertical Slice)** conforme abaixo.

Mantenha todas as informações existentes e altere apenas o necessário.

---

# 🗂️ Estrutura de Diretórios — Feature-First (Vertical Slice)
```bash
src/
├── features/
│   ├── users/
│   │   ├── http/
│   │   │   ├── UserController.js
│   │   │   └── userRoutes.js
│   │   ├── app/               # Casos de uso (Use Cases)
│   │   │   ├── CreateUserUseCase.js
│   │   │   ├── ListUsersUseCase.js
│   │   │   ├── GetUserByIdUseCase.js
│   │   │   ├── UpdateUserUseCase.js
│   │   │   └── DeleteUserUseCase.js
│   │   ├── domain/            # Entidades, regras de domínio e validações
│   │   │   └── User.js
│   │   ├── infra/             # Repositórios, gateways, providers
│   │   │   └── SqliteUserRepository.js
│   │   └── tests/             # Testes da feature
│   │       ├── unit/
│   │       └── integration/
│   └── auth/
│       ├── http/
│       ├── app/
│       ├── domain/
│       ├── infra/
│       └── tests/
├── shared/
│   ├── http/
│   │   └── middlewares/
│   ├── config/
│   └── utils/
└── index.js
```

---

# 📐 Convenções de Nomenclatura OBRIGATÓRIAS (Feature-First)

**Features:** `kebab-case`
* Ex.: `user-profile`, `reset-password`

**Use Cases:** `PascalCase` + `UseCase`
* Ex.: `CreateUserUseCase.js`, `ListUsersUseCase.js`

**Controllers:** `PascalCase` + `Controller`
* Ex.: `UserController.js`

**Routes:** `camelCase` + `Routes`
* Ex.: `userRoutes.js`

**Entidades de domínio:** `PascalCase`
* Ex.: `User.js`, `Product.js`

**Repositórios:** `PascalCase` + `Repository`
* Ex.: `SqliteUserRepository.js`

**Variáveis:** `camelCase`
* Ex.: `userName`, `userId`

**Constantes:** `SCREAMING_SNAKE_CASE`
* Ex.: `MAX_ATTEMPTS`, `API_URL`

---

# 🧠 Padrões Arquitetônicos — Vertical Slice

Cada **feature** é totalmente isolada e contém **todas as camadas** necessárias.

## 1. HTTP Layer (Controller + Routes)

**Local:** `features/<feature>/http/`

**Componentes:**
* Controllers
* Rotas
* Middlewares específicos da feature

**Regras:**
* Não contém lógica de negócio
* Não acessa banco de dados
* Apenas chama Use Cases
* Sempre usar try/catch
* Sempre usar `next(error)` para tratamento de erros
* Respostas padrão `{ success, data }`

**Exemplo - Controller:**
```javascript
// src/features/users/http/UserController.js
import CreateUserUseCase from "../app/CreateUserUseCase.js"
import ListUsersUseCase from "../app/ListUsersUseCase.js"
import GetUserByIdUseCase from "../app/GetUserByIdUseCase.js"
import UpdateUserUseCase from "../app/UpdateUserUseCase.js"
import DeleteUserUseCase from "../app/DeleteUserUseCase.js"

class UserController {
    async index(req, res, next) {
        try {
            const users = await ListUsersUseCase.execute()
            res.json({ success: true, data: users })
        } catch (err) {
            next(err)
        }
    }

    async show(req, res, next) {
        try {
            const user = await GetUserByIdUseCase.execute(req.params.id)
            res.json({ success: true, data: user })
        } catch (err) {
            next(err)
        }
    }

    async store(req, res, next) {
        try {
            const created = await CreateUserUseCase.execute(req.body)
            res.status(201).json({ success: true, data: created })
        } catch (err) {
            next(err)
        }
    }

    async update(req, res, next) {
        try {
            const updated = await UpdateUserUseCase.execute(req.params.id, req.body)
            res.json({ success: true, data: updated })
        } catch (err) {
            next(err)
        }
    }

    async destroy(req, res, next) {
        try {
            await DeleteUserUseCase.execute(req.params.id)
            res.status(204).send()
        } catch (err) {
            next(err)
        }
    }
}

export default new UserController()
```

**Exemplo - Routes:**
```javascript
// src/features/users/http/userRoutes.js
import { Router } from "express"
import UserController from "./UserController.js"

const router = Router()

router.get("/users", (req, res, next) => UserController.index(req, res, next))
router.get("/users/:id", (req, res, next) => UserController.show(req, res, next))
router.post("/users", (req, res, next) => UserController.store(req, res, next))
router.put("/users/:id", (req, res, next) => UserController.update(req, res, next))
router.delete("/users/:id", (req, res, next) => UserController.destroy(req, res, next))

export default router
```

---

## 2. App Layer (Use Cases)

**Local:** `features/<feature>/app/`

**Componentes:**
* Arquivos de caso de uso (um por operação)
* Orquestrações da lógica da feature

**Regras:**
* Contém TODA a lógica da feature
* Pode chamar múltiplos repositórios
* Não conhece HTTP/Express
* Não contém SQL
* Valida regras de negócio

**Exemplo - CreateUserUseCase:**
```javascript
// src/features/users/app/CreateUserUseCase.js
import UserRepository from "../infra/SqliteUserRepository.js"
import User from "../domain/User.js"

class CreateUserUseCase {
    async execute(data) {
        // Validação de dados de entrada (delegada ao Domain)
        const userEntity = new User(data)
        userEntity.validate()

        // Regras de negócio: verificar duplicidade
        const exists = await UserRepository.findByEmail(userEntity.email)
        if (exists) {
            const err = new Error("Email already registered")
            err.statusCode = 409
            throw err
        }

        const created = await UserRepository.create(userEntity.toJSON())
        return created
    }
}

export default new CreateUserUseCase()
```

**Exemplo - ListUsersUseCase:**
```javascript
// src/features/users/app/ListUsersUseCase.js
import UserRepository from "../infra/SqliteUserRepository.js"

class ListUsersUseCase {
    async execute() {
        return UserRepository.findAll()
    }
}

export default new ListUsersUseCase()
```

**Exemplo - GetUserByIdUseCase:**
```javascript
// src/features/users/app/GetUserByIdUseCase.js
import UserRepository from "../infra/SqliteUserRepository.js"

class GetUserByIdUseCase {
    async execute(id) {
        const user = await UserRepository.findById(id)
        if (!user) {
            const err = new Error("User not found")
            err.statusCode = 404
            throw err
        }
        return user
    }
}

export default new GetUserByIdUseCase()
```

**Exemplo - UpdateUserUseCase:**
```javascript
// src/features/users/app/UpdateUserUseCase.js
import UserRepository from "../infra/SqliteUserRepository.js"
import User from "../domain/User.js"

class UpdateUserUseCase {
    async execute(id, data) {
        // Verificar se usuário existe
        const exists = await UserRepository.findById(id)
        if (!exists) {
            const err = new Error("User not found")
            err.statusCode = 404
            throw err
        }

        // Validar dados (preservando o id)
        const userEntity = new User({ ...data, id })
        userEntity.validate()

        return UserRepository.update(id, userEntity.toJSON())
    }
}

export default new UpdateUserUseCase()
```

**Exemplo - DeleteUserUseCase:**
```javascript
// src/features/users/app/DeleteUserUseCase.js
import UserRepository from "../infra/SqliteUserRepository.js"

class DeleteUserUseCase {
    async execute(id) {
        const exists = await UserRepository.findById(id)
        if (!exists) {
            const err = new Error("User not found")
            err.statusCode = 404
            throw err
        }
        return UserRepository.delete(id)
    }
}

export default new DeleteUserUseCase()
```

---

## 3. Domain Layer

**Local:** `features/<feature>/domain/`

**Componentes:**
* Entidades
* Value Objects
* Regras de domínio puras
* Validações independentes

**Regras:**
* Zero dependências externas
* Nunca importa `infra`
* Nunca importa `http`
* Não usa Express
* Não usa SQLite

**Exemplo:**
```javascript
// src/features/users/domain/User.js
export default class User {
    constructor({ id = null, name, email, age } = {}) {
        this.id = id
        this.name = name
        this.email = email
        this.age = age
    }

    validate() {
        if (!this.name || this.name.trim().length < 3) {
            const err = new Error("Name must be at least 3 characters")
            err.statusCode = 400
            throw err
        }

        if (!this.email || !this.constructor.isValidEmail(this.email)) {
            const err = new Error("Invalid email")
            err.statusCode = 400
            throw err
        }

        if (!this.age || Number(this.age) < 18) {
            const err = new Error("Age must be at least 18")
            err.statusCode = 400
            throw err
        }
    }

    static isValidEmail(email) {
        return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)
    }

    toJSON() {
        return {
            id: this.id,
            name: this.name,
            email: this.email,
            age: this.age
        }
    }
}
```

---

## 4. Infra Layer

**Local:** `features/<feature>/infra/`

**Componentes:**
* Repositórios (ex.: SQLite, PostgreSQL)
* Gateways
* Providers
* Adapters externos

**Regras:**
* **Somente implementação concreta**
* SQL permitido apenas aqui
* Permite chamadas a APIs externas
* Não contém regra de negócio
* Interface clara para Use Cases

**Exemplo:**
```javascript
// src/features/users/infra/SqliteUserRepository.js
import db from "../../../shared/config/database.js"

class SqliteUserRepository {
    async findAll() {
        return db.all("SELECT id, name, email, age FROM users")
    }

    async findById(id) {
        return db.get("SELECT id, name, email, age FROM users WHERE id = ?", [id])
    }

    async findByEmail(email) {
        return db.get("SELECT id, name, email, age FROM users WHERE email = ?", [email])
    }

    async create(data) {
        const { name, email, age } = data
        const result = await db.run(
            "INSERT INTO users (name, email, age) VALUES (?, ?, ?)",
            [name, email, age]
        )
        return { id: result.lastID, name, email, age }
    }

    async update(id, data) {
        const { name, email, age } = data
        await db.run(
            "UPDATE users SET name = ?, email = ?, age = ? WHERE id = ?",
            [name, email, age, id]
        )
        return this.findById(id)
    }

    async delete(id) {
        return db.run("DELETE FROM users WHERE id = ?", [id])
    }
}

export default new SqliteUserRepository()
```

---

## 5. Tests Layer

**Local:** `features/<feature>/tests/`

**Contém:**
* Testes de Use Cases
* Testes de Controller
* Mocks de repositórios
* Testes unitários e de integração

**Exemplo - Teste Unitário:**
```javascript
// src/features/users/tests/unit/CreateUserUseCase.test.js
import CreateUserUseCase from "../../app/CreateUserUseCase.js"
import UserRepository from "../../infra/SqliteUserRepository.js"

jest.mock("../../infra/SqliteUserRepository.js")

test("should throw error for short name", async () => {
    await expect(
        CreateUserUseCase.execute({ name: "Jo", email: "jo@test.com", age: 25 })
    ).rejects.toThrow("Name must be at least 3 characters")
})

test("should throw error for duplicate email", async () => {
    UserRepository.findByEmail.mockResolvedValue({ id: 1, email: "test@test.com" })

    await expect(
        CreateUserUseCase.execute({ name: "João", email: "test@test.com", age: 25 })
    ).rejects.toThrow("Email already registered")
})
```

**Exemplo - Teste de Integração:**
```javascript
// src/features/users/tests/integration/userController.test.js
import request from "supertest"
import app from "../../../../index.js"
import db from "../../../../shared/config/database.js"

beforeAll(async () => {
    // Preparar DB (migrations / seed)
    await db.exec(`
        CREATE TABLE IF NOT EXISTS users (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            name TEXT NOT NULL,
            email TEXT UNIQUE NOT NULL,
            age INTEGER NOT NULL
        )
    `)
})

afterAll(async () => {
    await db.exec("DROP TABLE IF EXISTS users")
    await db.close()
})

test("POST /api/users - should create user", async () => {
    const res = await request(app)
        .post("/api/users")
        .send({ name: "João", email: "joao@test.com", age: 25 })

    expect(res.status).toBe(201)
    expect(res.body.success).toBe(true)
    expect(res.body.data).toHaveProperty("id")
})

test("GET /api/users/:id - should return user", async () => {
    // Criar usuário primeiro
    const createRes = await request(app)
        .post("/api/users")
        .send({ name: "Maria", email: "maria@test.com", age: 30 })

    const userId = createRes.body.data.id

    const res = await request(app).get(`/api/users/${userId}`)

    expect(res.status).toBe(200)
    expect(res.body.success).toBe(true)
    expect(res.body.data.name).toBe("Maria")
})
```

---

# 🔄 Shared Layer (Código Global)

**Local:** `src/shared/`

Contém apenas código reutilizável e livre de lógica de feature.

**Exemplo - Database Config:**
```javascript
// src/shared/config/database.js
import sqlite3 from "sqlite3"
import { open } from "sqlite"
import path from "path"
import { fileURLToPath } from "url"

const __dirname = path.dirname(fileURLToPath(import.meta.url))
const DB_PATH = process.env.DB_PATH || path.join(__dirname, "../../database.sqlite")

let db = null

async function getDatabase() {
    if (db) return db

    db = await open({
        filename: DB_PATH,
        driver: sqlite3.Database
    })

    await db.exec("PRAGMA foreign_keys = ON")
    return db
}

export default await getDatabase()
```

**Exemplo - Error Handler Middleware:**
```javascript
// src/shared/http/middlewares/errorHandler.js
export default function errorHandler(err, req, res, next) {
    console.error(err.stack)

    res.status(err.statusCode || 500).json({
        success: false,
        message: err.message || "Internal Server Error",
        ...(process.env.NODE_ENV === "development" && { stack: err.stack })
    })
}
```

---

# 🚀 Entry Point

**Exemplo:**
```javascript
// src/index.js
import express from "express"
import userRoutes from "./features/users/http/userRoutes.js"
import errorHandler from "./shared/http/middlewares/errorHandler.js"

const app = express()
const PORT = process.env.PORT || 3000

app.use(express.json())

// Montar rotas das features
app.use("/api", userRoutes)
// app.use("/api", authRoutes)

// Middleware final de erro
app.use(errorHandler)

app.listen(PORT, () => {
    console.log(`Server running on port ${PORT}`)
})

export default app
```

---

# 🔀 Fluxo Obrigatório no Vertical Slice
```
Request → Route → Controller → UseCase → Repository → Database/API
                                  ↓
Response ← Controller ← UseCase ← Repository ← Database/API
```

---

# 📑 Padrão de Respostas HTTP

**Sucesso (item único):**
```json
{
    "success": true,
    "data": { "id": 1, "name": "João" }
}
```

**Sucesso (lista):**
```json
{
    "success": true,
    "data": [
        { "id": 1, "name": "João" },
        { "id": 2, "name": "Maria" }
    ]
}
```

**Erro:**
```json
{
    "success": false,
    "message": "User not found"
}
```

---

# ✅ Checklist do Padrão Feature-First

- [ ] Cada feature tem suas próprias pastas (http, app, domain, infra, tests)
- [ ] Domínio isolado (sem dependências externas)
- [ ] Infra isolada (SQL apenas aqui)
- [ ] Sem cross-import entre features
- [ ] Regras de negócio apenas nos Use Cases
- [ ] Controllers sem lógica (apenas delegam para Use Cases)
- [ ] Um Use Case por operação (CreateUser, ListUsers, etc.)
- [ ] Somente `infra` acessa banco de dados
- [ ] Respostas HTTP padronizadas
- [ ] Nomes seguindo convenções estabelecidas
- [ ] Testes organizados dentro da feature

---

Depois que a IA criar/atualizar o arquivo, você revisa e pede ajustes:

```bash
Revise o openspec/project.md e ajuste:
- Adicione mais detalhes sobre [ASPECTO ESPECÍFICO]
- Corrija a descrição de [ITEM]
- Remova [INFORMAÇÃO DESNECESSÁRIA]
```

**Valide:** é importante você validar para seguir para o próximo passo.

```bash
openspec validate [change-id] --strict
```

Ex.:

```bash
openspec validate [add-user-registration] --strict
```
````
